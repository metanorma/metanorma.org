# How to use this template: 
# Within the document_attributes section, there are several key value pairs to model a reference entry. 
# A complete reference entry looks like this:
# nameOfTheAttribute: # Unique across document attributes for a flavor
#   tags: []  # A list of strings; Include flavor and type of attribute, e.g. "Author info"
#   title: "name of the attribute"
#   description: "Can contain inline and block AsciiDoc formatting". 
#   declaration: ":attribute:" #How the attribute is invoked
#   value_spec: "Comma-delimited list of classification tokens, expressed as `type:value` pairs. The `type:` prefix is optional, where omitted `default:` prefix is auto-supplied"
#   value_default: null  #OPTIONAL A string showing literal value for this attribute, without quotes. If the attribute doesn't take a values, this stays empty.
#   required: true #Describes if the attribute is required to generate a well-formed doc or optional. Takes a boolean.
#   added_in_version: v1.9.1 # Could be auto-inferred, however the renderer might run into trouble, see https://github.com/metanorma/metanorma.org/wiki/Flavor-reference-structure-specification
#   example: >- ...  #OPTIONAL Should be pure AsciiDoc. Will be rendered as is inside an example block.
#   more_information: "string" #OPTIONAL Use to provide related links, such as a blog article that discusses background info, if applicable

version: v1.x.x #@opoudjis Check versions throughout this doc. Huuuge thanks!

document_attributes:
#--- Build and validation ---#
  nodoc: 
    tags: ["standoc", "Build and validation"] 
    title: "No document generation" #@opoudjis Review how we should actually name :nodoc:
    description: "TBD: Explain what “nodoc” is used for".
    value_spec: "Does not take values" #@opoudjis verify claim
    required: false
    added_in_version: v1.x.x
    example: >- 
              Optional code sample using AsciiDoc syntax
    more_information: >-
              Optional: Provide a link to resources that explain this attribute. 
              If we do not have content on this topic, but you think we need it, let @kickoke or @techtrailhead know. 
  
  novalid:
    tags: ["standoc", "Build and validation"]
    title: "document type"
    description: "TBD: Explain what “novalid” is used for".
    value_spec: "Does not take values" #@opoudjis verify claim
    required: false
    added_in_version: v1.x.x
    example: >- 
              Optional code sample using AsciiDoc syntax
    more_information: >-
              Optional: Provide a link to resources that explain this attribute
              If we do not have content on this topic, but you think we need it, let @kickoke or @techtrailhead know. 
  
  #--- Bibliographic lookup ---#
  no-isobib: 
    tags: ["standoc", "Bibliographic lookup"] 
    title: "name of the attribute"
    description: "Can contain inline and block AsciiDoc formatting". 
    declaration: ":no-isobib:" 
    value_spec: "Comma-delimited list of classification tokens, expressed as `type:value` pairs. The `type:` prefix is optional, where omitted `default:` prefix is auto-supplied"
    value_default: null  
    required: false 
    added_in_version: v1.x.x
     example: >- 
              Optional code sample using AsciiDoc syntax
    more_information: >-
              Optional: Provide a link to resources that explain this attribute
              If we do not have content on this topic, but you think we need it, let @kickoke or @techtrailhead know. 
  
#--- Please include all Bibliographic lookup references from https://www.metanorma.org/author/topics/building/reference-lookup/ ---#

  nameOfTheAttribute: 
    tags: ["standoc", "Bibliographic lookup"] 
    title: "name of the attribute"
    description: "Can contain inline and block AsciiDoc formatting". 
    declaration: ":attribute:" 
    value_spec: "Comma-delimited list of classification tokens, expressed as `type:value` pairs. The `type:` prefix is optional, where omitted `default:` prefix is auto-supplied"
    value_default: null  #optional, depending if and what values the attr takes
    required: false 
    added_in_version: v1.x.x
     example: >- 
              Optional code sample using AsciiDoc syntax
    more_information: >-
              Optional: Provide a link to resources that explain this attribute
              If we do not have content on this topic, but you think we need it, let @kickoke or @techtrailhead know. 
  
  #--- Caches ---#
      #--- Is there some reference information hidden in https://www.metanorma.org/author/topics/building/reference-lookup/#lookup-result-caching ? ---#

  nameOfTheAttribute: 
    tags: ["standoc", "Caches"] 
    title: "name of the attribute"
    description: "Can contain inline and block AsciiDoc formatting". 
    declaration: ":attribute:" 
    value_spec: "Comma-delimited list of classification tokens, expressed as `type:value` pairs. The `type:` prefix is optional, where omitted `default:` prefix is auto-supplied"
    value_default: null  #optional, depending if and what values the attr takes
    required: false 
    added_in_version: v1.x.x
     example: >- 
              Optional code sample using AsciiDoc syntax
    more_information: >-
              Optional: Provide a link to resources that explain this attribute
              If we do not have content on this topic, but you think we need it, let @kickoke or @techtrailhead know. 
  



  #--- Math ---#
      #--- Please include all references from https://www.metanorma.org/author/topics/document-format/text/#mathematical-expressions ---#
  nameOfTheAttribute: 
    tags: ["standoc", "Math"] 
    title: "name of the attribute"
    description: "Can contain inline and block AsciiDoc formatting". 
    declaration: ":attribute:" 
    value_spec: "Comma-delimited list of classification tokens, expressed as `type:value` pairs. The `type:` prefix is optional, where omitted `default:` prefix is auto-supplied"
    value_default: null  #optional, depending if and what values the attr takes
    required: false 
    added_in_version: v1.x.x
     example: >- 
              Optional code sample using AsciiDoc syntax
    more_information: >-
              Optional: Provide a link to resources that explain this attribute
              If we do not have content on this topic, but you think we need it, let @kickoke or @techtrailhead know. 
  


  #--- Languages and localization ---#
      #--- Please include all references from https://www.metanorma.org/author/topics/languages/ ---#
  nameOfTheAttribute: 
    tags: ["standoc", "Languages and localization"] 
    title: "name of the attribute"
    description: "Can contain inline and block AsciiDoc formatting". 
    declaration: ":attribute:" 
    value_spec: "Comma-delimited list of classification tokens, expressed as `type:value` pairs. The `type:` prefix is optional, where omitted `default:` prefix is auto-supplied"
    value_default: null  #optional, depending if and what values the attr takes
    required: false 
    added_in_version: v1.x.x
     example: >- 
              Optional code sample using AsciiDoc syntax
    more_information: >-
              Optional: Provide a link to resources that explain this attribute
              If we do not have content on this topic, but you think we need it, let @kickoke or @techtrailhead know. 
  


  #--- Document info ---#
  nameOfTheAttribute: 
    tags: ["standoc", "Document info"] 
    title: "name of the attribute"
    description: "Can contain inline and block AsciiDoc formatting". 
    declaration: ":attribute:" 
    value_spec: "Comma-delimited list of classification tokens, expressed as `type:value` pairs. The `type:` prefix is optional, where omitted `default:` prefix is auto-supplied"
    value_default: null  #optional, depending if and what values the attr takes
    required: false 
    added_in_version: v1.x.x
     example: >- 
              Optional code sample using AsciiDoc syntax
    more_information: >-
              Optional: Provide a link to resources that explain this attribute
              If we do not have content on this topic, but you think we need it, let @kickoke or @techtrailhead know. 
  


  #--- URIs ---#
  nameOfTheAttribute: 
    tags: ["standoc", "URIs"] 
    title: "name of the attribute"
    description: "Can contain inline and block AsciiDoc formatting". 
    declaration: ":attribute:" 
    value_spec: "Comma-delimited list of classification tokens, expressed as `type:value` pairs. The `type:` prefix is optional, where omitted `default:` prefix is auto-supplied"
    value_default: null  #optional, depending if and what values the attr takes
    required: false 
    added_in_version: v1.x.x
     example: >- 
              Optional code sample using AsciiDoc syntax
    more_information: >-
              Optional: Provide a link to resources that explain this attribute
              If we do not have content on this topic, but you think we need it, let @kickoke or @techtrailhead know. 




  #--- Timestamps ---#
  nameOfTheAttribute: 
    tags: ["standoc", "Timestamps"] 
    title: "name of the attribute"
    description: "Can contain inline and block AsciiDoc formatting". 
    declaration: ":attribute:" 
    value_spec: "Comma-delimited list of classification tokens, expressed as `type:value` pairs. The `type:` prefix is optional, where omitted `default:` prefix is auto-supplied"
    value_default: null  #optional, depending if and what values the attr takes
    required: false 
    added_in_version: v1.x.x
     example: >- 
              Optional code sample using AsciiDoc syntax
    more_information: >-
              Optional: Provide a link to resources that explain this attribute
              If we do not have content on this topic, but you think we need it, let @kickoke or @techtrailhead know. 
  



  #--- Author info ---#
  nameOfTheAttribute: 
    tags: ["standoc", "Author info"] 
    title: "name of the attribute"
    description: "Can contain inline and block AsciiDoc formatting". 
    declaration: ":attribute:" 
    value_spec: "Comma-delimited list of classification tokens, expressed as `type:value` pairs. The `type:` prefix is optional, where omitted `default:` prefix is auto-supplied"
    value_default: null  #optional, depending if and what values the attr takes
    required: false 
    added_in_version: v1.x.x
     example: >- 
              Optional code sample using AsciiDoc syntax
    more_information: >-
              Optional: Provide a link to resources that explain this attribute
              If we do not have content on this topic, but you think we need it, let @kickoke or @techtrailhead know. 
  


  #--- Visual appearance ---#
  nameOfTheAttribute: 
    tags: ["standoc", "Visual appearance"] 
    title: "name of the attribute"
    description: "Can contain inline and block AsciiDoc formatting". 
    declaration: ":attribute:" 
    value_spec: "Comma-delimited list of classification tokens, expressed as `type:value` pairs. The `type:` prefix is optional, where omitted `default:` prefix is auto-supplied"
    value_default: null  #optional, depending if and what values the attr takes
    required: false 
    added_in_version: v1.x.x
     example: >- 
              Optional code sample using AsciiDoc syntax
    more_information: >-
              Optional: Provide a link to resources that explain this attribute
              If we do not have content on this topic, but you think we need it, let @kickoke or @techtrailhead know. 
  
